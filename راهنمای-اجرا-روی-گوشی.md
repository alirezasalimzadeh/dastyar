# دستیار مشاور — راهنمای راه‌اندازی کامل (سرور + دیتابیس + چند حساب جدا)

> نسخهٔ جدید: **سایت روی سرور شما + دیتابیس PostgreSQL تمیز + چندین حساب کاربری کاملاً جدا از هم.**
> همهٔ داده‌ها فقط در دیتابیس ذخیره می‌شوند — چیزی جدا از دیتابیس نیست.
>
> این راهنما را قدم‌به‌قدم انجام دهید. کل راه‌اندازی اولیه: **حدود ۴۵ دقیقه**.

---

## ۱. چه چیزی لازم دارید؟

| مورد | مقدار | توضیح |
|---|---|---|
| سرور (VPS) | ۲ هسته / ۴ گیگ رم / ۴۰ گیگ SSD / Ubuntu 24.04 | هر ارائه‌دهنده‌ای — هاستینگ ایرانی (پارس‌پک/آروان/زرین‌پال) یا خارجی (Hetzner/DigitalOcean) |
| دامنه | دلخواه (مثلاً `dastyar.example.com`) | **اختیاری** — اول با آی‌پی کار می‌کنید، بعداً اضافه می‌کنید |
| لپ‌تاپ شما | همان که همیشه باهاش کار می‌کنید | ساختِ نسخهٔ سایت روی لپ‌تاپ است، نه سرور |

---

## ۲. بخش اول — راه‌اندازی سرور (یک‌بار، ~۳۰ دقیقه)

### ۲.۱) ورود به سرور
پس از خرید VPS، ارائه‌دهنده به شما آی‌پی (مثلاً `203.0.113.45`) و رمز کاربر `root` می‌دهد.
از ترمینال لپ‌تاپ:

```bash
ssh root@203.0.113.45
```

### ۲.۲) کپی کد به سرور
```bash
apt update && apt install -y git
git clone <آدرس-ریپازیتوری-دستیاری> /opt/dastyar
cd /opt/dastyar
git checkout <نام-شاخهٔ-جدید>      # همان شاخه‌ای که آخرین تغییرات توش push شده (من می‌گویم)
```
(آدرس ریپازیتوری = همان آدرسی که روی گیت‌هاب است؛ اگر خصوصی است از `git clone https://oauth2:TOKEN@github.com/...` استفاده کنید.)

### ۲.۳) تنظیمات سرور
```bash
cd /opt/dastyar/server
cp .env.example .env
nano .env
```
دو مقدار را ویرایش کنید:

```env
POSTGRES_PASSWORD=یک-رمز-قوی-خودتان
SITE_URL=203.0.113.45        # آی‌پی سرور (بعداً دامنه)
```

سپس:

```bash
bash deploy.sh
```

این اسکریپت خودکار: Docker را نصب می‌کند ← کلیدهای امنیتی JWT را می‌سازد ← دیتابیس PostgreSQL ۱۶ را با **اسکیمای تمیز چندکاربره** (جدول‌ها + RLS جداسازی حساب‌ها + seed جغرافیایی) بالا می‌آورد ← GoTrue (ثبت‌نام/ورود) و PostgREST (API داده) و Caddy (وب‌سرور) را راه می‌اندازد.

در پایان پیام «✅ کامل شد» می‌بینید. آدرس `http://آی‌پی-سرور` را باز کنید — صفحهٔ «🔧 سرور آماده است» می‌آید. **سرور آماده است، سایت هنوز آپلود نشده است** — بخش بعد.

> 📌 از فایل `server/.env` سرور، مقدار `ANON_KEY` را کپی کنید و کنار دستتان بگذارید — بخش بعد لازم می‌شود.

---

## ۳. بخش دوم — آپلود سایت (روی لپ‌تاپ)

### ۳.۱) تنظیم متغیرها (یک‌بار)
روی لپ‌تاپ، در پوشهٔ پروژه:

```bash
export DEPLOY_HOST=203.0.113.45
export DEPLOY_USER=root
export DEPLOY_DIR=/opt/dastyar/server/www
export VITE_SUPABASE_URL=http://203.0.113.45
export VITE_SUPABASE_ANON_KEY=مقداری-که-از-server/.env-سرور-کپی-کردید
```

> این exportها فقط برای همین ترمینال می‌مانند. هر بار ترمینال جدید باز کردید، دوباره بزنید
> (یا در آخر `~/.bashrc` بنویسید تا ماندگار شود).

### ۳.۲) ساخت + آپلود
```bash
bash scripts/deploy-vps.sh
```
این اسکریپت: `npm ci` ← `npm run build` (با آدرس سرور شما) ← `rsync` کل `dist/` به سرور.

حالا `http://203.0.113.45` را باز کنید → **صفحهٔ ورود/ثبت‌نام دستیار مشاور**.

---

## ۴. بخش سوم — ساخت حساب‌ها و استفاده

1. در صفحهٔ اول، **«حساب کاربری جدید»** را بزنید → ایمیل + رمز عبور + نام/نام‌خانوادگی/موبایل.
2. چون سرور بدون سرور ایمیل تنظیم شده، **ایمیل تأیید لازم نیست** — همان لحظه وارد می‌شوید.
3. هر کس (شما، شریک، همکار) همین کار را می‌کند — **هر حساب دنیای خودش را دارد**:
   - مشتری‌ها، فایل‌ها، مالک‌ها، تگ‌ها، پیگیری‌ها… هر حساب **فقط** داده‌های خودش را می‌بیند.
   - جداسازی در خود دیتابیس است (RLS) — نه در ظاهر برنامه. حتی با SQL مستقیم هم دادهٔ حساب دیگری دیده نمی‌شود.
   - تگ‌های ۱۶ تاییِ پیش‌فرض برای هر حساب جدید خودکار ساخته می‌شوند.
4. از گوشی هم همان آدرس را باز کنید — یک حساب جدا روی گوشی هم می‌توانید داشته باشید (با مرورگر یا PWA).

> ⚠️ **مهم:** دادهٔ نسخهٔ قبلی (محلی/ابر) به این دیتابیس جدید منتقل **نمی‌شود** — طبق تصمیم شما شروع تمیز است.
> برای کار با دادهٔ جدید باید رکورد بسازید (اول مشتری، بعد فایل — یا بالعکس؛ موتور تطبیق همان است).

---

## ۵. آپدیتِ سایت (هر تغییری که در کد می‌دهیم)

هر بار که تغییر جدیدی push شد:

```bash
git pull
bash scripts/deploy-vps.sh
```

(فقط اگر `package.json` عوض شده باشد `npm ci` لازم است — اسکریپت خودش انجام می‌دهد.)

---

## ۶. آپگرید به دامنه + HTTPS (وقتی دامنه خریدید)

1. در ارائه‌دهندهٔ دامنه، رکورد **A** دامنه → آی‌پی سرور.
2. روی سرور:
   ```bash
   cd /opt/dastyar/server
   nano .env                      # SITE_URL=دامنه-شما
   cp Caddyfile.https.example Caddyfile
   docker compose restart auth web
   ```
3. در لپ‌تاپ: `export VITE_SUPABASE_URL=https://دامنه-شما` و بعد `bash scripts/deploy-vps.sh`.
4. Caddy گواهی HTTPS را **خودکار** از Let's Encrypt می‌گیرد — کاری لازم نیست.

---

## ۷. پشتیبان‌گیری (backup) — مهم!

کل دیتای شما در یک فایل volume داکر است. هر هفته (یا هر روز) یک خط کافی است:

```bash
# روی سرور — ساخت بکاپ کامل دیتابیس:
docker compose -f /opt/dastyar/server/docker-compose.yml exec -T db \
  pg_dump -U supabase_admin -Fc postgres > /opt/dastyar-backup-$(date +%F).dump
```

بکاپ را هر از گاهی به جایی بیرون از سرور (گوگل‌درایو/لپ‌تاپ) کپی کنید.
بازگردانی هم ساده است: `docker compose down` ← volume را پاک ← dump را با `pg_restore` وارد کنید (اگر لازم شد راهنمایی می‌دهم).

---

## ۸. جدول عیب‌یابی سریع

| نشانه | دلیل / راه‌حل |
|---|---|
| صفحهٔ «سرور آماده است» می‌آید | سایت آپلود نشده → `bash scripts/deploy-vps.sh` روی لپ‌تاپ |
| صفحه سفید / «VITE_SUPABASE_URL تنظیم نشده» | سایت بدون env ساخته شده → دوباره deploy-vps.sh (مطمئن شوید exportها زده شده) |
| ثبت‌نام: «Internal server error» | `docker compose logs auth` روی سرور — معمولاً SITE_URL در .env غلط است |
| لاگین می‌شود ولی داده نمی‌آید | `docker compose logs rest` + `curl http://آی‌پی/rest/v1/` — باید JSON بیاید |
| «password authentication failed» در لاگ‌ها | `.env` را بعد از ساخت کلیدها عوض کرده‌اید → `docker compose up -d` |
| HTTPS با دامنه کار نمی‌کند | رکورد A دامنه هنوز propagated نشده (تا ۳۰ دقیقه) + پورت 80/443 باز باشد |

---

## ۹. ساختار سرور (نمای کلی)

```
VPS شما
├── Caddy (:80/:443)
│    ├── /          → سایت (dist — static)
│    ├── /rest/v1   → PostgREST ←── supabase-js در مرورگر
│    └── /auth/v1   → GoTrue (ثبت‌نام/ورود)
└── PostgreSQL 16
     ├── جدول‌های کاری  ←  ستون user_id + سیاست RLS «فقط ردیف‌های خود»
     │   (customers, properties, owners, calls, follow_ups, deals, tasks, …)
     ├── profiles     ←  فهرست همکاران (خواندن مشترک، ویرایش فقط خود)
     ├── tags         ←  دیکشنری تگِ جدا برای هر کاربر
     └── جغرافیا       ←  استان/شهرستان/شهر/محله (مشترک، seed شده)
```

---

## ۱۰. اگر یک روز خواستید برگردید به نسخهٔ ابری (Supabase)

ساختار API یکسان است — فقط `VITE_SUPABASE_URL` و `VITE_SUPABASE_ANON_KEY` را با مقادیر supabase.co عوض کنید و دوباره deploy کنید. (البته باید migrations ابری را هم اجرا کنید — فعلاً لازم نیست.)
