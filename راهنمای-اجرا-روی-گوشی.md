# دستیار مشاور — راهنمای اجرا روی گوشی (نسخهٔ کاملاً محلی)

> **نسخهٔ فعلی: هیچ سروری در کار نیست.** کل برنامه و کل داده‌ها داخل خودِ گوشی می‌مانند.
> بدون اینترنت کار می‌کند، هزینهٔ ماهیانه ندارد، و به هیچ سرویس خارجی وابسته نیست.

---

## ۱. این نسخه چگونه کار می‌کند؟

- برنامه یک **PWA** است (برنامهٔ تحت وب که مثل اپ نصب می‌شود).
- همهٔ داده‌ها (مشتری‌ها، فایل‌ها، مالک‌ها، پیگیری‌ها و…) در **دیتابیس داخلی مرورگرِ همین گوشی** ذخیره می‌شود.
- ورود/خروج و ثبت‌نام **حذف شده** — با باز کردن برنامه، همه‌چیز آماده است.
- موتور تطبیق، داشبورد و همهٔ بخش‌ها دقیقاً مثل قبل کار می‌کنند؛ فقط جای سرور، دیتابیس داخلی گوشی آمده است.

---

## ۲. نصب روی گوشی (فقط یک‌بار، ~۵ دقیقه)

### مرحلهٔ ۱ — روشن کردن سرور فایل روی لپ‌تاپ
روی لپ‌تاپ (همان ترمینال همیشگی):

```bash
cd ~/Desktop/dastyar_moshaver/project-bolt-sb1-kmnzhm3k/project
git pull
bash روشن-کردن-سرور.sh
```

این اسکریپت آدرس را نمایش می‌دهد، مثلاً: `http://192.168.187.123:4173`

### مرحلهٔ ۲ — باز کردن در گوشی
- گوشی و لپ‌تاپ باید به **یک وای‌فای** وصل باشند.
- آدرس بالا را در **کروم**ِ گوشی باز کنید.
- برنامه کامل بارگذاری می‌شود.

### مرحلهٔ ۳ — نصب
- در کروم: منوی سه‌نقطه → **«به صفحهٔ اصلی اضافه شود»** (Add to Home screen)
- نام «دستیار مشاور» را بگذارید → افزودن
- حالا آیکون برنامه روی صفحهٔ اصلی است — مثل یک اپ.

**از این به بعد، حتی بدون اینترنت و بدون لپ‌تاپ، برنامه کامل کار می‌کند.**

> چرا مرحلهٔ اول لازم است؟ فقط برای بار اول — که فایل‌های برنامه یک‌بار دانلود و داخل گوشی کش می‌شوند. بعد از آن، اینترنت و لپ‌تاپ دیگر لازم نیست.

---

## ۳. پشتیبان‌گیری — مهم‌ترین عادت

چون داده‌ها روی خود گوشی است، **پیش از هر تغییر گوشی / حذف مرورگر / پاک‌کردن داده‌ها، حتماً بکاپ بگیرید**:

1. داخل برنامه: **تنظیمات → پشتیبان‌گیری**
2. یک فایل JSON ساخته و دانلود می‌شود — آن را در گوگل‌درایو یا جایی امن بگذارید

**بازیابی** (روی گوشی جدید یا بعد از پاک‌شدن):
تنظیمات → بازیابی → همان فایل JSON را انتخاب کنید → همهٔ داده‌ها برمی‌گردند (جایگزینی کامل).

> ⚠️ «پاک‌کردن داده‌های مرورگر» در تنظیمات گوشی = پاک‌شدن کل برنامه. همیشه بکاپ داشته باشید.

---

## ۴. چند حساب جدا

| سناریو | راه‌حل |
|---|---|
| شما روی گوشی خودتان، شریک روی گوشی خودش | **کاملاً جدا** — هر گوشی دنیای خودش را دارد |
| دو حساب روی یک گوشی | در کروم: منو → **پروفایل‌ها** → پروفایل دوم بسازید؛ در هر پروفایل جدا، PWA را نصب کنید — داده‌ها هیچ‌گاه با هم قاطی نمی‌شوند |

---

## ۵. آپدیتِ برنامه (هر بار که تغییری می‌دهیم)

1. لپ‌تاپ: `git pull && bash روشن-کردن-سرور.sh`
2. گوشی را به همان وای‌فای وصل کنید و **یک‌بار آدرس را در کروم باز کنید**
3. نسخهٔ جدید خودکار جایگزین می‌شود — آیکون صفحهٔ اصلی همان است، داده‌ها دست‌نخورده می‌مانند

(اگر گوشی آفلاین باشد، نسخهٔ قبلی باقی می‌ماند — هیچ‌چیز خراب نمی‌شود.)

---

## ۶. اگر روزی خواستید اپ واقعی (APK) داشته باشید

مسیرش باز است: با Android Studio روی لپ‌تاپ، همین نسخه را داخل یک فایل APK می‌بندیم که بدون مرورگر هم اجرا شود و آیکون و نوتیفیکیشن واقعی داشته باشد. هر وقت خواستید، بگویید.

---

## ۷. اگر روزی سرور خریدید

بستهٔ سرور کامل (دیتابیس پستگرس + چند حساب جدا روی یک سرور) در همین ریپازیتوری آماده است (`server/`). با یک دستور، می‌توانید از نسخهٔ محلی به نسخهٔ سروری بروید و داده‌ها را با بکاپ JSON وارد کنید. فعلاً کاری لازم نیست.
